version: "3.9"

services:
  compass:
    image: zhoukq/compass:latest
    ports:
      - "3000:3000"
    environment:
      COMPASS_ADMIN_TOKEN: change-me
      COMPASS_SESSION_SECRET: change-me-too
      PORT: 3000
    volumes:
      - ./docker/config.yaml:/app/public/config.yaml
